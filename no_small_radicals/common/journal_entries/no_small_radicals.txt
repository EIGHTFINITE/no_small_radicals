je_no_small_radicals = {
	icon = "gfx/interface/icons/politics_icons/radicalized.dds"

	group = je_group_crises

	is_shown_when_inactive = {
		is_player = yes
		NOT = { has_global_variable = no_small_radicals_active }
	}

	possible = {
		hidden_trigger = {
			is_player = yes
			NOT = { has_global_variable = no_small_radicals_active }
		}
	}

	immediate = {
		if = {
			limit = {
				is_player = yes
				NOT = { has_global_variable = no_small_radicals_active }
			}
			trigger_event = { id = no_small_radicals.1 }
		}
	}

	complete = {
		has_global_variable = no_small_radicals_active
	}

	weight = 0
}
